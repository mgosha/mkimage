[pytest]
testpaths = tests
markers =
    needs_root: test requires root/sudo for mount operations
    windows: test requires SSH access to Windows VM (winvm)
    macos: test requires SSH access to macOS host
